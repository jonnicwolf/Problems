const wave = str => {
//make each letter capitalized and push the new word to a new array
    let sample = [...str]
    for (let i = 0; i < sample.length; i++){
        const cap = sample[i].toUpperCase()
        sample = []

    }

}

console.log(wave('wow'))